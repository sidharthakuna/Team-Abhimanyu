# Points the frontend at your FastAPI backend.
# Default (no .env needed) is http://localhost:8000 — copy this file to
# `.env` ONLY if your backend runs somewhere else (a different port,
# a deployed URL, Docker, etc).

VITE_API_BASE=http://localhost:8000
